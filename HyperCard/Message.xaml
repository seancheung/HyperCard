<Window x:Class="HyperCard.Message"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" MinHeight="100" MinWidth="300"
        Height="100" Width="300"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_DragMove"
        BorderBrush="{StaticResource Color_A}" BorderThickness="0.8"
        Background="{StaticResource Color_A0}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/MetroButton.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Canvas Grid.ColumnSpan="4" Background="{StaticResource Color_A}">
            <Button Name="Close" Style="{StaticResource ButtonClose}" Canvas.Right="5" Canvas.Top="0" Width="22" Height="22" Click="Close_Click"/>
        </Canvas>
    </Grid>
</Window>
